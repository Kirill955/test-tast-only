export interface ITheme {
  dark: {
    colors: {
      background: string;
      text: string;
      blockBackground: string;
      blockText: string;
      blockTitle: string;
    };
  };
  light: {
    colors: {
      background: string;
      text: string;
      blockBackground: string;
      blockText: string;
      blockTitle: string;
    };
  };
  sizes: {
    x1: string;
    x2: string;
    x3: string;
    x4: string;
    x5: string;
    x6: string;
    x7: string;
    x8: string;
  };
}
